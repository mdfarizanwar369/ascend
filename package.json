{
  "name": "ascend",
  "version": "0.1.0",
  "private": true,
  "description": "Ascend fitness accountability SaaS MVP",
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "setup": "npm install",
    "dev": "concurrently \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "build": "npm run build --workspace shared && npm run build --workspace backend && npm run build --workspace frontend",
    "test": "npm run build --workspace shared && npm run test --workspace backend",
    "lint": "npm run lint --workspace frontend && npm run lint --workspace backend",
    "migrate": "npm run db:migrate --workspace backend",
    "seed": "npm run db:seed --workspace backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
